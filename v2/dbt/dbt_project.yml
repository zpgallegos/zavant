name: zavant_analytics
version: 1.0.0
config-version: 2

profile: zavant_analytics

model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  zavant_analytics: {}

vars:
  current_projection_contract_version: "zavant-analytical-game-projection/v1"
